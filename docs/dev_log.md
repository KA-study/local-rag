## 2026/05/25 - Initialize program_files/

### Conclusion
- Initialized the `program_files/` directory and committed initial configuration and code changes together.

### Reason
- The `program_files/` directory was initially misconfigured as a Git submodule, so it was converted into a normal directory.
- At the time of initialization, some implementation code had already been added, making it difficult to separate the commit into smaller units.
- As a result, configuration and code changes were committed in a single step.

### Details
- The following files were committed at the same time:
  - `pdf/loader.py`
  - `pdf/factory.py`
  - `pdf/chunker.py`
  - `pdf/base.py`
